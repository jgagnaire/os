to assemble the bootloader, compile the kernel and create the bootable floppy:
'make'


to run :
-> with Qemu: 'qemu-system-x86_64 -fda floppy'
-> or Bochs (and the bochsrc config file): 'bochs'

If Bochs gives an error related to the SDL module, install the 'bochs-sdl' package